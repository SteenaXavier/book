import 'package:flutter/material.dart';

class ReviewPage extends StatelessWidget {

  
  @override
  Widget build(BuildContext context) {
    return Scaffold(

    body: Column(
      children: [Row(
        children: [Container(),Column(
          children: [Text("College diaries at GEC",style: TextStyle(fontSize: 43),),
                     Text("Vipin E",style: TextStyle(fontSize: 23),)],)],
      ),Container()],),
    
    )
      

    ;
  }
}